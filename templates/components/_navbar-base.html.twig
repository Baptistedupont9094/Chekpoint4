{% set route_name = app.request.attributes.get('_route') %}

<nav class="navbar navbar-dark bg-primary navbar-expand-md">
  <a class="navbar-brand" href="/">
    <h2 class="text-light p-2 m-0" style="font-family: 'Permanent Marker', cursive; text-decoration: none;">Refund <span class="text-light">Me</span></h2>
  </a>

  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbar-top" aria-controls="navbar-top" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbar-top">
    <ul class="navbar-nav w-100">
        {% for link in links %}
            <li class="nav-item {{ route_name == link.route_name ? 'active' : '' }}">
                <a class="nav-link d-flex" href="{{ path(link.route_name) }}">
                    <i class="material-icons">{{ link.icon }}</i>
                    <p class="m-0 ml-2">{{ link.label }}</p>
                </a>
            </li>
        {% endfor %}

        <div class="flex-grow-1"></div>

        <li class="nav-item">
            <a class="nav-link d-flex" href="{{ path('app_logout') }}" title="Déconnexion">
                <i class="material-icons text-danger">power_settings_new</i>
            </a>
        </li>
    </ul>
  </div>
</nav>